import { FC } from 'react';
import { NotFoundBlock } from '../components';

const NotFound:FC = () => {
    return (
        <>
          <NotFoundBlock/>
        </>
    );
};

export default NotFound;